#!/usr/bin/env bash
set -e

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

# Activate venv if present
if [ -d "$ROOT/.venv" ]; then
    source "$ROOT/.venv/bin/activate"
fi

# Inject repo context
export PYTHONPATH="$ROOT"

cd "$ROOT"

python -m OSPM.Plotting.plot_analysis1
