# OSPM_Config_Segue1_Test
# Galaxy-only diagnostic configuration

from pathlib import Path
from Data.Data_Prep.Data_Paths import build_data_paths

Galaxy = "Segue1"

OSPM_ROOT    = Path(__file__).resolve().parent
PROFILE_ROOT = OSPM_ROOT

CONFIG = {
    # Identity
    "GALAXY": Galaxy,

    # Geometry (declared, never fitted)
    "RA0_DEG":         151.7667,
    "DEC0_DEG":        16.0819,
    "DISTANCE_PC":     23000.0,
    "PA_DEG":          None,
    "AXIS_RATIO_Q":    1.0,
    "R_HALF_LIGHT_PC": 29.0,
    "R_MAX_STARS_PC":  120.0,
    "INCLINATION_DEG": 90.0,

    # Parameter space (gentle)
    "PARAMETER_NAMES": ["rho_s", "r_s", "MBH"],
    "INITIAL_THETA":   [0.5, 20.0, 1000.0],
    "THETA_BOUNDS": [
        (0.05, 5.0),     # rho_s
        (10.0, 150.0),   # r_s
        (0.0, 3e5),      # MBH
    ],

    # Data selection (tight, stable)
    "RADIUS_DEG":  0.30,
    "RUWE_MAX":    1.4,
    "PAR_SNR_MIN": 5.0,

    # Column authority
    "STAR_R_COL":    "r_pc",
    "STAR_V_COL":    "vlos",
    "STAR_VERR_COL": "vlos_err",
    "RA_COL":        "ra",
    "DEC_COL":       "dec",
    "VLOS_COL":      "vlos",

    # Galaxy-local physical scales
    "MIN_DISTANCE":     5e-4,
    "MAX_DISTANCE":     2e3,
    "POTENTIAL_EXTENT": 6.0,

    # Paths
    **build_data_paths(PROFILE_ROOT),
}
